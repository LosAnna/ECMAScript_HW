// Дан массив const arr = [1, 5, 7, 9] с помощью Math.min и spread оператора,
// найти минимальное число в массиве, решение задания должно состоять из одной строки

const arr = [1, 5, 7, 9];
const minNumber = Math.min(...arr);
console.log(minNumber);
